import { app } from './config.app.local';
import { database } from './config.database.local';
import { mail } from './config.mail.local';
import { web } from './config.web';

export const config = {
    app,
    database,
    mail,
    web
};